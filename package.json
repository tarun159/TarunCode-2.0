{
  "name": "taruncode-2.0",
  "version": "2.0.0",
  "description": "Premium futuristic lab-program platform",
  "private": true,
  "scripts": {
    "dev": "npm run dev --prefix client",
    "build": "npm run build --prefix client",
    "build:client": "npm run build --prefix client",
    "preview": "npm run preview --prefix client"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}